% ЛР3: тестовые факты и запросы для предметной области «Университет»

% Экземпляры
instance_of(alice, Undergraduate).
instance_of(bob, Graduate).
instance_of(dr_smith, Professor).
instance_of(cs101, Course).

% Связи
teaches(dr_smith, cs101).
takes(alice, cs101).
takes(bob, cs101).

% Ожидаемые проверки:
% query(instance_of(alice, Person)).
% query(instance_of(bob, Person)).
% query(instance_of(dr_smith, Person)).
% query(takes(alice, cs101)).
% query(teaches(dr_smith, cs101)).
